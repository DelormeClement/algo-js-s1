var random = Math.random()
alert(random)


class Pokemon {
    construtor(Name, attack, defense, HP, luck,){
        this.Name = Name
        this.attack = attack
        this.defense = defense
        this.hp = HP
        this.luck = luck
        this.speed
    }
}

let Ponita = new Pokemon(Ponita, 20, 5, 50, 0.7,)
let BigChungus = new Pokemon(BigChungus, 25, 10, 60, 0.3)


isLuke(luck)
     if (Pokemon.luck >= Math.random) {
    isLuke = true
}   else {
        isLuke = false
    }

if (HP <= 0) {
    console.log(Pokemon) = console.log(Pokemon) + " est hors de combat !!"
}

while (Pokemonn[this.hp] <= 0) {
    if (isLuke.Pokemon[0] = true) {
        (Pokemon[1].HP + Pokemon[1].def) -= Pokemon[0].atk  
        console.log((Pokemon[1].Name) + " a " + Pokemon[1].HP + " point de vie restant")
     } else {
        break
     }
     if (isLuke.Pokemon0[1] = true) {
        (Pokemon[0].HP + Pokemon[0].def) -= Pokemon[1].atk  
        console.log((Pokemon[0].Name) + " a " + Pokemon[0].HP + " point de vie restant")
     } else {
        break
     }
}